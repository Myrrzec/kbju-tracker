# Used by `vite build` (what Cloudflare Pages runs) for production deploys.
# Must match the Render service's actual URL — if Render had to rename the
# service (name already taken), update this to match and redeploy.
VITE_API_BASE_URL=https://kbju-tracker-api.onrender.com
